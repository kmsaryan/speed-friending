# Database configuration
DATABASE_URL="/data/speed-friending.sqlite"

# Server port - Render assigns this automatically, but we'll set it just in case
PORT=10000

# URLs and environment
FRONTEND_URL="https://speed-friending-ui.onrender.com"
NODE_ENV="production"

# Optional: Set this if you want to configure Socket.IO timeout
SOCKET_TIMEOUT=20000

# Optional: Set this for additional security
CORS_ORIGIN="https://speed-friending-ui.onrender.com"
