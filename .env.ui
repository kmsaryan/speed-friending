# API endpoint for the React app to connect to 
REACT_APP_BACKEND_URL="https://speed-friending-api.onrender.com"

# Set to production for optimized build
NODE_ENV="production"

# Optional: Configure the port for development (not used in production)
PORT=3000
